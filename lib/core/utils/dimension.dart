import 'package:get/get.dart';

class Dimensions{
  static double screenHeight = Get.context!.height;
  static double screenWidth = Get.context!.height;

  static double pageView = screenHeight / 2.689;
  static double pageViewContainer = screenHeight / 3.765;
  static double pageViewTextContainer = screenHeight / 6.275;
}